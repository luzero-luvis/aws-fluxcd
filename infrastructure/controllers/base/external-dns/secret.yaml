apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: do-token
  namespace: external-dns

spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: do-token
  data:
    - secretKey: DO_TOKEN
      remoteRef:
        key: kv/data/external-dns
        property: DO_TOKEN
